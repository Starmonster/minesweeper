{
  "name": "minesweeper-project",
  "version": "1.0.0",
  "description": "Transpile code for minesweeper project",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src -d lib"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-preset-env": "^1.6.0"
  }
}
